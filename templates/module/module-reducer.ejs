import {Map} from 'immutable';
// Import Constant
import {EXAMPLE} from './constant';

// Initial State
const initialState = Map();

const <%= name %>Reducer = (state = initialState, action) => {
  const {type, demo} = action;
  switch (action.type) {
    case EXAMPLE:
      return state.set('demo', demo);
    default:
      return state;
  }
};

export default <%= name %>Reducer;
